# Custom Rules

- **Auto-Fix Minor Syntax Errors**: If the user makes trivial syntax errors (like missing semicolons, unbalanced brackets, or minor typos), automatically fix them in the code yourself instead of blocking and asking the user to do it. This acts as a quality-of-life exception to the "user writes 100% of the code" mentor rule.
